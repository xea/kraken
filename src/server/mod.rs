pub struct ThreadedServer {}

impl ThreadedServer {
    pub fn new() -> Self {
        Self::default()
    }
}

impl Default for ThreadedServer {
    fn default() -> Self {
        Self {
        }
    }
}